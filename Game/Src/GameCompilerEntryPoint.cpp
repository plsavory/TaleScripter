#include "GameCompiler/GameCompiler.hpp"
#include <iostream>

int main() {
  std::cout<<"Game compiler test, builds the required database tables..."<<std::endl;

  GameCompiler *compiler = new GameCompiler();
}
